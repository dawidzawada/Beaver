import React from "react";
import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export const CodeOverviewScreen = () => {
  return (
    <SafeAreaView>
      <Text>CodeOverviewScreen</Text>
    </SafeAreaView>
  );
};
